# pytest-homeassistant-custom-component pins homeassistant, pytest,
# pytest-asyncio and friends to the exact versions Home Assistant core tests
# against. Pinning any of them here as well only creates resolution conflicts,
# so this file deliberately lists just the two direct tools.
pytest-homeassistant-custom-component
ruff
