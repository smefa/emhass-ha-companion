# Agent instructions

This repo uses a **private shared agent memory** (gitignored) for Cursor and Claude Code.

1. Read `.agents/memory/MEMORY.md` if it exists on this machine.
2. Open any linked notes that apply to the task.

Do not commit `.agents/`. Do not put secrets or live-host details in
tracked files. Update notes under `.agents/memory/` instead of inventing
parallel Cursor-only or Claude-only memories.
